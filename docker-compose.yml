services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:////data/vydaje.db
    volumes:
      - vydaje-data:/data

volumes:
  vydaje-data:
